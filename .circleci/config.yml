version: 2.1
orbs:
  newman: postman/newman@1.0.0
jobs:
  newman-collection-run-env-timeout:
    executor: newman/postman-newman-docker
    steps:
      - checkout
      - newman/newman-run:
          collection: ./newman/collection.json
          environment: ./newman/environment.json
          timeout: '1000'